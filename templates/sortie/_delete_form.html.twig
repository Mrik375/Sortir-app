<form method="post" action="{{ path('app_sortie_edit', {'id': sortie.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette sortie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie.id) }}">
    <button type="submit" name="delete" class="btn btn-danger">Supprimer</button>
</form>
